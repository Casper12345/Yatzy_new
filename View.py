""" This is the view module"""


class PrintStatements(object):
    pass # maybe these will not be written as classes(objects), but rather as a set of functions


class UserInput(object):
    pass # same goes for here